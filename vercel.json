{
  "version": 2,
  "buildCommand": "echo \"No frontend build on Vercel; backend runs from api/*.ts serverless functions\"",
  "outputDirectory": "public",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/",
      "destination": "/api/health"
    }
  ]
}
